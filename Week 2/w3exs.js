const getLarger = (a,b) => {
    return a > b ? a : b
}

// console.log(getLarger(9,5))

const findSignOfProduct = (a,b,c) => {
    return (a*b*c) >= 0 ? "The sign is + " : "The sign is -"
}

// console.log(findSignOfProduct(1,-2,3))

